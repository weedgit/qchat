# Qchat brand icon

`qchat-icon-512.png` is the canonical Rchat icon source.

```bash
./scripts/sync-brand-icons.sh
```

XinChat uses separate green **X** assets:

```bash
./scripts/generate-xinchat-icons.sh
```
