{
  "name": "qchat-desktop",
  "version": "0.1.0",
  "private": true,
  "main": "main.js",
  "scripts": {
    "start": "ELECTRON_DISABLE_SANDBOX=1 electron . --no-sandbox",
    "start:sandbox": "electron .",
    "pack": "electron-builder --dir"
  },
  "devDependencies": {
    "electron": "^43.1.1"
  }
}
