# Browser must reach LiveKit on the VM LAN IP (not Cursor-forwarded localhost).
# UDP media cannot use Cursor TCP port-forward. Adjust if the VM IP changes.
NEXT_PUBLIC_LIVEKIT_URL=ws://192.168.91.136:7880
NEXT_PUBLIC_API_URL=http://localhost:8080
